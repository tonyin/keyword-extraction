#!/usr/bin/env python

import pandas
import zipfile

def nb_train(train):

	counts = {}

	return counts

def naive_bayes(train, pred):
    
	counts = nb_train(train)

    return pred

# import string
# puncs = list(string.punctuation)
# for s in train['Tags'].values:
#     for kw in s
#         for c in kw:
#             if c in puncs:
#                 puncs.remove(c)
# puncs = ''.join(puncs)